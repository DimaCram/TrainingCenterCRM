@model List<TrainingCenterCRM.BLL.DTO.GroupDTO>
@{
    ViewData["Title"] = "Groups";
}

<div class="content">
    <h1 class="content__header">Groups</h1>
    <div class="containt-fluid">
        <div class="row">
            <div class="col-sm-3"></div>
            <div class="col-sm-6">
                <div class="table-responsive">
                    <table class="table table-striped table-hover text-center">
                        <thead class="thead-dark">
                            <tr>
                                <th scope="col">Name</th>
                                <th scope="col">Start date</th>
                                <th scope="col"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var group in Model)
                            {

                                <tr>
                                    <th scope="col">@group.Name</th>
                                    <th scope="col">@group.StartDate.ToString("dd.MM.yyyy")</th>
                                    <th scope="col">
                                        <a class="btn btn-success m-1" asp-controller="Groups" asp-action="EditGroup" asp-route-id="@group.Id">Edit</a>
                                        <a class="btn btn-danger m-1" asp-controller="Groups" asp-action="DeleteGroup" asp-route-id="@group.Id">Delete</a>
                                    </th>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <a class="btn btn-primary" asp-controller="Groups" asp-action="AddGroup">Add new group</a>
            </div>
        </div>
        <div class="col-sm-3"></div>
    </div>
</div>